<span class="space-for-icon-validation"></span>
<input type="{{$type}}"
    class="{{$class}}"
    id="{{$id}}"
    name="{{$name}}"
    placeholder="{{$placeholder}}"
    value="{{ $value }}"
    disabled
>
