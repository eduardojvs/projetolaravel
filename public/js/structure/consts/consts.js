
export const DATATABLE_LIST_FORMAT    = 0;
export const DATATABLE_EXTERNO_FORMAT = 1;
export const DATATABLE_duplicar_FORMAT = 1;

export const FORMAT_DATATABLE = '<f<t>ipl><"clear">';

export const ID_FROM_URL_CRUD = window.location.pathname.split('/')[2];

export const URL_PATH_API = 'http://' + window.location.host + '/api';
